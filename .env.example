# Server-only base URL. The browser only calls the same-origin /api/im proxy.
IM_API_BASE_URL=http://ec2-43-200-143-21.ap-northeast-2.compute.amazonaws.com:8090

# Keep true for the self-contained UI demo. Set false for live iM Server data.
NEXT_PUBLIC_DEMO_MODE=true
